// Type your code here, or load an example.
int simple_ops(int num) {
    int number = 90;
    number = number + num;
    int output = num * num;
    output = output << 2; 
    return output * number;
}